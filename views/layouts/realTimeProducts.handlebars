<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    
    <script> 
        const {io} = require("../app.js"); 
        const {postProduct, deleteProduct} = require("../productsControllers.js");
        const socket = io();

      socket.on ("productoCreado", (data) =>{
        console.log(${data});
      });

      socket.on ("productoEliminado", (data) =>{
        console.log(${data});
      });
      
        
   
       
         </script>
</body>
</html>